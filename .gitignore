node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.data/
# Local environment secrets
.env
.env.local
package-lock.json.replit-backup

# Replit artifact dump: pasted prompts + third-party sample code.
# Contained a real Stripe test key; kept out of the repo entirely.
attached_assets/

# Fulfillment outputs — never commit customer/product data
sample.csv
export.csv
*.deliverable.csv
