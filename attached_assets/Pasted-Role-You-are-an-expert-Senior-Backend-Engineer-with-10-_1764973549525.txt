Role: You are an expert Senior Backend Engineer with 10+ years of experience building high-scale data acquisition platforms.

Context: We are building the "ZoomInfo for College Athletics." We currently have a React/Node.js/PostgreSQL app on Replit. We have implemented a basic scraper, but we need to upgrade it to a Commercial-Grade Ingestion Engine capable of handling 1,100+ university websites.

Project Goal: Build a production-ready crawling infrastructure to extract structured staff data (Name, Title, Email, Phone, Bio Image) from university athletic directories. Some pages are static; many (Sidearm Sports) use lazy loading or client-side rendering.

Constraints & Requirements:

Language: Node.js & TypeScript (Must integrate with existing server/ structure).

Database: PostgreSQL with Drizzle ORM (Use existing shared/schema.ts).

Scalability: Support parallel processing (Worker Pool) but strictly enforce per-domain rate limiting (politeness) to avoid IP bans.

Reliability: Implement "Smart Retries" (exponential backoff) and "Dead Letter Queues" for failed URLs.

Anti-Blocking:

User-Agent Rotation.

Request Throttling.

Handling "Sidearm Sports" specific DOM structures (tables vs. cards).

Rendering: Use Playwright (already installed).

Observability: We need logs and extraction metrics (e.g., "Hit Rate: % of contacts with emails").

Deliverables:

Architecture Blueprint: Describe the flow of data from Seed List -> Job Queue -> Browser Pool -> Parser -> Database.

Concrete Stack Upgrade:

Use p-limit or bullmq for queue management.

Use cheerio for fast parsing after Playwright renders.

Use winston or similar for structured logging.

Code Skeletons (TypeScript):

ScraperWorker: The class responsible for checking out a browser page, navigating, and handling errors.

ParserFactory: A strategy pattern to select the right parser (Generic vs. Sidearm vs. WordPress) based on the URL.

HealthMonitor: A simple service to track success/fail rates.

Configuration Strategy:

Create a scraper-config.ts to define selectors for different site types (e.g., .sidearm-staff-member vs .staff-directory-row).

Operational Runbook:

How to add a new "Parser Strategy" when a school changes its website layout.

How to handle a "Cloudflare Block" (manual intervention vs. proxy integration).

Acceptance Criteria:

Provide actual, runnable TypeScript code that integrates with my existing storage.ts.

The scraper must handle the "Oregon State" edge case (titles in text nodes) we previously identified.

Show how to save "Extraction Metadata" (time taken, HTTP status) into the extraction_jobs table.

Please start by outlining the Architecture Blueprint and then providing the code for the Configurable Parser Strategy.