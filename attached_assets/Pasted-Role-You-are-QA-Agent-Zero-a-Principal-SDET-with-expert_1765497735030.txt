Role: You are QA-Agent-Zero, a Principal SDET with expertise in high-scale data platforms. You are auditing the "Athletics Directory" application before its commercial B2B launch.

The Tech Stack:

Frontend: React, Tailwind, TanStack Query (React Query), Shadcn UI.

Backend: Node.js, Express, Playwright (Scraper), Google Gemini (AI).

Database: PostgreSQL (Drizzle ORM) with Full-Text Search.

Infra: Replit Container (Limited RAM/CPU).

The Mission: Perform a "Deep Stack" Audit. Your goal is to identify Structural Weaknesses, Unhandled Edge Cases, and UX Dead Ends.

Please execute the following 6-Phase Forensic Audit:

Phase 1: The "Day Zero" & Seeding Integrity (Database & Logic)
Test 1.1 (Idempotency): Click the "Initialize Power 5 Database" button. While the spinner is spinning, reload the page or open a second tab and click it again.

Pass Criteria: The database should NOT contain duplicate schools. The backend should handle the unique constraint violation gracefully.

Test 1.2 (Cold Start): Delete all rows in school_directories. Load the dashboard. Does the "Initialize" button reappear correctly?

Test 1.3 (Status Consistency): If a seed fails halfway, does the UI show "Pending" or "Failed"? Does the retry button work?

Phase 2: The "Chaos Monkey" Scraper Test (Backend & Infra)
Test 2.1 (Concurrency Limit): Trigger a "Conference Extraction" for the SEC (16 schools). Immediately trigger "Big Ten" (18 schools).

Monitor: Check the server logs. Does p-limit correctly hold the queue at 2-3 concurrent jobs? Or does the server crash with OOM (Out of Memory)?

Test 2.2 (The "Oregon State" Regression): Run a single extraction for "Oregon State".

Verification: Check the database. Do the records have Titles? (Verifies the specific parser fix).

Test 2.3 (AI Fallback): Manually insert a HTML snippet with no classes but valid text into the scraper test harness. Does aiExtractFallback trigger? Does the contact get the confidence: { source: 'ai' } flag?

Phase 3: The B2B Workflow & Data Flow (Frontend & API)
Test 3.1 (Large Payload Attack): In the "List Matcher", upload a CSV with 5,000 rows.

Pass Criteria: The browser should not freeze. The API should accept the batch or return 413 Payload Too Large (handling it gracefully).

Test 3.2 (Ambiguity): Upload a list containing just "Miami".

Pass Criteria: Does the UI prompt for clarification (Miami FL vs Miami OH)? Or does it blindly guess?

Test 3.3 (Export Encoding): Export a list containing a name with an accent (e.g., "José"). Open the CSV. Is it José or JosÃ©? (Tests UTF-8 BOM headers).

Phase 4: API Security & Rate Limiting (Security)
Test 4.1 (Unauthorized Access): Use curl to hit POST /api/v1/enrich without a Bearer Token. Expect 401 Unauthorized.

Test 4.2 (Rate Limit Check): Run a loop sending 200 requests in 1 minute to /api/v1/match.

Pass Criteria: The 101st request should return 429 Too Many Requests. The server should not crash.

Test 4.3 (SQL Injection): In the "Search" bar, input ' OR 1=1; --. Verify Drizzle ORM sanitizes this inputs (Result should be 0 matches, not All matches).

Phase 5: UI/UX & Responsive Behavior (Frontend)
Test 5.1 (Modal State): Open the "School Detail Modal". Click "Extract". Close the modal immediately. Re-open it. Is the button state correct (Spinning)?

Test 5.2 (Mobile View): Resize window to 375px (iPhone).

Check: Does the "Sidebar" collapse into a Hamburger menu?

Check: Is the "Data Grid" horizontally scrollable?

Test 5.3 (Empty States): Filter by a Conference with 0 schools. Does it show a helpful "No Results" message or a blank white screen?

Phase 6: Code Quality & Architecture (Static Analysis)
Audit 6.1: Check server/routes.ts. Are we waiting for await inside a map loop (serial processing) instead of Promise.all (parallel processing)?

Audit 6.2: Check client/src/lib/queryClient.ts. Are we using staleTime correctly, or is the app refetching data every time the user clicks a tab (causing UI flicker)?

Report Format: For every failure, provide:

Severity: (Critical / High / Low)

Location: (File Name)

Reproduction: (1-2-3 Steps)

Recommended Fix: (Code snippet or Logic change)

Start by executing Phase 1 (Seeding) and Phase 2 (Scraper Concurrency). Report findings immediately.