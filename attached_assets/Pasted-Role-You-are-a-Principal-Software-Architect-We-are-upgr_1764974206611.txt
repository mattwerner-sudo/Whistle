Role: You are a Principal Software Architect. We are upgrading this prototype into a Commercial-Grade AI Data Engine.

Current State: We have a basic scraper, a job queue, and a React frontend. Problem: The scraper is brittle. If a website changes its HTML layout (like Oregon State moving titles), the scraper fails. Goal: Implement an "Intelligent Hybrid Extraction" pipeline.

Please execute the following 4 steps to upgrade the application:

Step 1: Create the AI Extraction Service
Create server/lib/ai-extractor.ts. This service will use the Google Gemini API to parse complex HTML contacts when standard Regex fails.

TypeScript

// server/lib/ai-extractor.ts
import { GoogleGenerativeAI } from "@google/generative-ai";

// Initialize Gemini (Replit usually provides the API key in secrets)
const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY || "");

export async function extractWithAI(htmlSnippet: string) {
  try {
    if (!process.env.GEMINI_API_KEY) return null;

    const model = genAI.getGenerativeModel({ model: "gemini-1.5-flash" });
    
    const prompt = `
      Analyze this HTML snippet from a university staff directory. 
      Extract the following fields for the staff member found: Name, Title, Email, Phone.
      Return ONLY a JSON object. Do not include markdown formatting.
      If a field is not found, use null.
      
      HTML Snippet:
      ${htmlSnippet.substring(0, 2000)} // Limit context size
    `;

    const result = await model.generateContent(prompt);
    const response = result.response;
    const text = response.text();
    
    // Clean markdown if present
    const jsonStr = text.replace(/```json/g, '').replace(/```/g, '').trim();
    return JSON.parse(jsonStr);
  } catch (error) {
    console.error("AI Extraction Failed:", error);
    return null;
  }
}
Step 2: Integrate AI into the Main Scraper
Update server/staffExtractor.ts. Modify the extractPersonFromContainer function to use the AI service as a fallback when confidence is low.

Logic to implement:

Run the existing DOM/Cheerio extraction.

Check the result. If name is valid but title or email is missing (Confidence < 60):

Call extractWithAI(container.outerHTML).

Merge the AI result with the DOM result (preferring AI for missing fields).

Step 3: Implement the "Browser Pool"
Create server/lib/browser-pool.ts to manage memory usage efficiently, preventing the app from crashing during bulk scrapes.

TypeScript

import { chromium, Browser } from "playwright";

let globalBrowser: Browser | null = null;

export async function getBrowserPage() {
  if (!globalBrowser) {
    console.log("[BrowserPool] Launching Global Browser...");
    globalBrowser = await chromium.launch({ 
      headless: true,
      args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage'] 
    });
  }
  // Create a fresh context for each page to avoid cookie pollution
  const context = await globalBrowser.newContext({
    userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'
  });
  return context.newPage();
}
Step 4: Finalize the "Commercial Dashboard"
Update client/src/pages/staff-directory.tsx. Ensure it uses the School Grid layout with the Detail Modal we discussed.

Feature: Add a "Data Quality" badge to the modal. If AI was used to extract data, display a " ✨ AI Enhanced" badge next to the staff count.

Deployment Instruction: After generating the files, run npm install @google/generative-ai in the shell to enable the AI features.