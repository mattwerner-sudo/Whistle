You are QA-Agent-X. I have just refactored the application to use a Modal-Based Architecture for school details. Instead of navigating to a new page, users now open a Dialog window to view staff.

Shutterstock
Explore

Please perform a Deep Component Audit on client/src/pages/staff-directory.tsx to verify the new SchoolDetailDialog.

Execute this 4-Point Test Plan:

Test 1: Data Fetching Isolation (Performance Check)
Context: We moved the staff query inside the modal logic.

Action: Open the browser Network Tab. Click on "University of Alabama".

Verification:

Pass: You should see exactly ONE network request to /api/staff/members?schoolId=alabama.

Fail: If you see requests for other schools or a request before the modal opens, the enabled: !!selectedSchoolId logic is broken.

Stress Test: Rapidly click "View Details" on Alabama, close it, then click "Florida". Verify the table clears and reloads correctly (no data leaking between schools).

Test 2: The "Extraction" Loop
Action: Open a school that has status: pending (gray badge).

Action: Inside the Modal, click the "Extract Data" button.

Verification:

The button should show a spinner (extractMutation.isPending).

Crucial: Close the modal immediately after clicking. Look at the card on the grid behind it.

Expected: The card's badge should eventually turn to PROCESSING (blue) or SUCCESS (green) because we invalidated the schools query.

Test 3: Modal State & Layout
Action: Open a school with many staff members (e.g., a Power 5 school).

Verification:

Does the modal have a scrollbar? (Check max-h-[85vh] class).

Does the "Visit Directory" link open in a new tab (target="_blank")?

Does pressing the ESC key close the modal and reset selectedSchoolId to null?

Test 4: Regression Testing (Filters)
Action: Select "SEC" from the sidebar. Search for "Florida".

Action: Open the Florida card.

Verification:

Does the background Grid stay filtered to SEC/Florida while the modal is open?

When you close the modal, are your search filters still preserved?

Start by reviewing the SchoolDetailDialog code block I just added. Does the Dialog component correctly handle the onOpenChange event to reset the state?